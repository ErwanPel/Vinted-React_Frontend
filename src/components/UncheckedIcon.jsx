import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

export default function UncheckedIcon() {
  // This component create the arrow  for the toggle on the search of the header
  return <FontAwesomeIcon icon="arrow-up" />;
}
